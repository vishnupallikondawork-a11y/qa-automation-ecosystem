services:

  selenium-chrome:

    image: selenium/standalone-chrome

    ports:
      - "4444:4444"

  qa-automation:

    build: .

    depends_on:
      - selenium-chrome

    volumes:
      - ./test_runs:/app/test_runs

    command: ["smoke"]